using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class ValidationPanelScript : MonoBehaviour
{
    [SerializeField]
    TextMeshPro temporalTMP;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //GameObject.Find("ErasableText").GetType();
        //TextMeshPro tmp = FindObjectOfType<TextMeshPro>();
        //var x = GameObject.FindGameObjectsWithTag("tempTextField");
    }
}
